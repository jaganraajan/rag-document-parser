receivers:
  otlp:
    protocols:
      http:

exporters:
  debug: {}   

processors:
  batch:

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]